{# src\Devschool\CinemaBundle\Resources\views\Realisateur\show.html.twig #}
{% extends 'DevschoolCinemaBundle::layout.html.twig' %}

{% block title %}Liste des Films {% endblock %}

      {% block body %}

      <div class="container">

          <div class="row">

              <div class="col-md-3">
                  <p class="lead">Cinéma</p>
                  <div class="list-group">
                      <a href="films" class="list-group-item">Films</a>
                      <a href="realisateurs" class="list-group-item active">Réalisateurs</a>
                  </div>
              </div>

              <h2>Liste des Films</h2>
          <div class="col-md-9">

            <br />

            {% for film in films %}

              <div class="thumbnail">
                <div class="caption-full">
                  <h4 class="pull-right">{{film.genre}}</h4>
                  <h4>{{ film.titre|upper }}</h4>
                  <p>{{film.synopsis}}</p>
                  <div class="ratings">
                      <p class="pull-right">sorti le {{film.dateDeSortie | date('d/m/Y')}}</p>
                  </div>
                </div>

              </div>
              {% endfor %}

            </div>
          </div>
        </div>
      {% endblock %}
